apiVersion: v1
kind: ConfigMap
metadata:
  name: payment
  namespace: prints
data:
  CART_HOST: "cart"
  CART_PORT: "8080"
  USER_HOST: "user"
  USER_PORT: "8080"
  AMQP_HOST: "rabbitmq"
  AMQP_USER: "roboshop"
  AMQP_PASS: "roboshop123"
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: payment
  namespace: prints
  # these are replica set labels
  labels:
    project: prints
    componenet: payment
    tier: backend
spec:
  replicas: 1
  selector:
    # these labels are used to select pods
    matchLabels:
      project: prints
      componenet: payment
      tier: backend
  # pod definition
  template:
    metadata:
      labels:
        project: prints
        componenet: payment
        tier: backend
    spec:
      containers:
      - name: payment
        image: peruripramod/payment:v2
        imagePullPolicy: Always
        envFrom:
        - configMapRef:
             name: payment
        ports:
        - name: liveness-port
          containerPort: 8080
        readinessProbe:
          httpGet:
            path: /health
            port: liveness-port
          failureThreshold: 1
          periodSeconds: 15
        livenessProbe:
          httpGet:
            path: /health
            port: liveness-port
          failureThreshold: 1
          periodSeconds: 15
---
apiVersion: v1
kind: Service
metadata:
  name: payment
  namespace: prints
spec:
  selector:
    project: prints
    componenet: payment
    tier: backend
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8080